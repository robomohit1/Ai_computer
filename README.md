# AI Computer Agent

## Native run

```bash
uvicorn app.main:app --reload
```

## Docker run

```bash
docker compose up --build
```

## VNC

Connect to `vnc://localhost:5900` on macOS or use TigerVNC on Linux/Windows.

## Setup

Install browser runtime:

```bash
playwright install chromium
```
